version: '3.8'

services:
  hotel-penguin:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./backend/data:/app/backend/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
